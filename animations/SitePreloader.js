import { select } from "utils/utils";
import { gsap } from "gsap";

export class Preloader {
    constructor(el) {
        this.filterEl = select('#zap-out');
    }

    animateFilter() {
        
    }
}